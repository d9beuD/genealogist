{
	# Basic app server config.
	# Symfony handles routing; Caddy serves public/ and forwards PHP to FrankenPHP.
	order php_server before file_server
}

:80 {
	root * /var/www/public
	encode zstd gzip
	php_server
	file_server
}
