{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-end">
        <button class="btn btn-primary">{{ button_label|default('action.save'|trans) }}</button>
    </div>
{{ form_end(form) }}
